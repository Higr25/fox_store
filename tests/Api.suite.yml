actor: ApiTester
modules:
  enabled:
    - REST:
        depends: PhpBrowser
        url: 'http://localhost:8000/api/v1/'
    - Db:
        dsn: "mysql:host=%database.%;dbname=rmaserver2"
        user: "root"
        password: "%DB_PASSWORD%"
        cleanup: true
    - Tests\Support\Helper\BaseTest
